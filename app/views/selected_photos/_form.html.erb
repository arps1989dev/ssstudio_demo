<%= form_for(@selected_photo) do |f| %>
  <% if @selected_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@selected_photo.errors.count, "error") %> prohibited this selected_photo from being saved:</h2>

      <ul>
      <% @selected_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :photo_id %><br>
    <%= f.number_field :photo_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
